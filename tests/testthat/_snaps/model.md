# sav_model() with full post-hoc works

    Code
      res3_a <- sav_model(df_ok2, method_pa = "gam", limitation = "custom")
    Message
      i Looking for depth and fetch in column names.
      i Using secchi, limitation, and substrate for post-hoc treatment.

